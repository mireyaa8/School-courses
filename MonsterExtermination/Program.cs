namespace MonsterExtermination
{
    internal class Program
    {
        static void Main(string[] args)
        {
            int[] armor = Console.ReadLine()
                .Split(",")
                .Select(int.Parse)
                .ToArray();
            int[] strike = Console.ReadLine()
                .Split(",")
                .Select(int.Parse)
                .ToArray();
            int count = 0;
            for(int i =0; i< armor.Length; i++) 
            { 

            } 



        }
    }
}